import { describe, it } from "vitest";

// don't forget to implement the in-memory repository and the use case

describe("Create Transaction Use Case", () => {
    it("should be able to create a new transaction", async () => {

    });
});